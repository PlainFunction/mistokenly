{{- if .Values.postgres }}
{{- if .Values.postgres.deploy }}
apiVersion: v1
kind: Service
metadata:
  name: postgres-mq
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: postgres-mq
{{- end }}
{{- end }}